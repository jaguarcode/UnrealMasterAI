{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "MCP Tool Manifest Schema",
  "description": "Schema for MCP tool registration definitions",
  "type": "object",
  "required": ["name", "description", "inputSchema"],
  "properties": {
    "name": {
      "type": "string",
      "pattern": "^[a-z]+\\.[a-zA-Z]+$",
      "description": "Tool name in dot-notation (e.g., 'blueprint.serialize')"
    },
    "description": {
      "type": "string",
      "description": "Human-readable tool description for LLM context"
    },
    "inputSchema": {
      "type": "object",
      "description": "JSON Schema for tool input parameters"
    },
    "category": {
      "type": "string",
      "enum": ["editor", "blueprint", "slate", "compilation", "file"],
      "description": "Tool category for RAG grouping"
    },
    "destructive": {
      "type": "boolean",
      "default": false,
      "description": "Whether this tool requires human approval"
    },
    "cacheable": {
      "type": "boolean",
      "default": false,
      "description": "Whether results can be cached in CacheStore"
    }
  }
}
