{
  "name": "unreal-master-agent",
  "version": "0.1.0",
  "private": true,
  "workspaces": ["mcp-server"],
  "scripts": {
    "build": "npm run build --workspace=mcp-server",
    "test": "npm run test --workspace=mcp-server",
    "typecheck": "npm run typecheck --workspace=mcp-server"
  }
}
